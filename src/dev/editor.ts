// DEV-only level editor overlay (compiled out of production). Filled in at milestone 4.
export function initEditor(_cv: HTMLCanvasElement, _ctx: CanvasRenderingContext2D) {
  console.log('editor: not yet implemented');
}
